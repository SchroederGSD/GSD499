#pragma strict

internal var iMode = false; //flag whether inventory mode is off or on

function Start () {

}

function Update () {
//	if (Input.GetKeyDown ("space") || Input.GetKeyDown ("enter"))
//		//start game which is GameScene
//		Application.LoadLevel(GameScene);
}